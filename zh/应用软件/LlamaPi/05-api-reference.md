# API 参考

这里是一段 API 参考说明文本。

## 核心类

### `测试类`

```python
class 测试类:
    def __init__(
        self,
        参数一: str,
        参数二: int = 123,
        参数三: float = 0.5,
        参数四: bool = False,
    ):
        """
        这里是一段 docstring 注释。

        Args:
            参数一: 这里是参数说明
            参数二: 这里是参数说明
            参数三: 这里是参数说明
            参数四: 这里是参数说明
        """
        ...
```

### `测试类.测试方法()`

```python
def 测试方法(
    self,
    输入: str,
    最大长度: int = 256,
    流式输出: bool = False,
) -> str | Generator[str, None, None]:
    """
    这里是一段 docstring 注释。

    Args:
        输入: 这里是参数说明
        最大长度: 这里是参数说明
        流式输出: 这里是参数说明

    Returns:
        这里是返回值说明
    """
    ...
```

## 工具函数

### `工具函数一()`

```python
def 工具函数一(参数甲: str, 参数乙: int) -> dict:
    """
    这里是一段 docstring 注释。
    """
    ...
```

### `工具函数二()`

```python
def 工具函数二(参数甲: list) -> str:
    """
    这里是一段 docstring 注释。
    """
    ...
```

## 异常类型

| 异常 | 说明 |
|------|------|
| `测试异常一` | 这里是异常说明文字 |
| `测试异常二` | 这里是异常说明文字 |
| `测试异常三` | 这里是异常说明文字 |
